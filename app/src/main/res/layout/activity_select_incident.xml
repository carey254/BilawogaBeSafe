<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:gravity="center"
    android:background="@drawable/background">

    <!-- Spinner for Incidents -->
    <Spinner
        android:id="@+id/incidentSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:minHeight="48dp" />

    <!-- Manual input if needed -->
    <EditText
        android:id="@+id/manualIncidentEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/incidentSpinner"
        android:layout_marginTop="20dp"
        android:background="@drawable/edittext_background"
        android:hint="Or describe your emergency..."
        android:minHeight="48dp" />

    <!-- Confirm Button -->
    <Button
        android:id="@+id/confirmButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirm Emergency"
        android:layout_below="@id/manualIncidentEditText"
        android:layout_marginTop="20dp"
        android:onClick="confirmIncident" />
</RelativeLayout>
