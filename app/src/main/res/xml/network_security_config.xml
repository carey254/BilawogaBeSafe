<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- SECURITY: Enforce HTTPS for all network traffic -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">maps.googleapis.com</domain>
    </domain-config>
    
    <!-- SECURITY: Block all cleartext traffic by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    
    <!-- SECURITY: No certificate pinning for Google to avoid breakage -->
</network-security-config> 